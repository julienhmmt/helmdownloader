---
# Local SonarQube Community Build server and one-shot scanner for helmdownloader.
# Use with the matching Makefile targets (sonar-up, sonar-scan, sonar-down, ...).

services:
  sonarqube:
    image: sonarqube:26.6.0.123539-community
    container_name: helmdownloader-sonarqube
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      # Disable Elasticsearch bootstrap checks so the container starts locally
      # without tuning vm.max_map_count on Docker Desktop / OrbStack.
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - helmdownloader_sonarqube_data:/opt/sonarqube/data
      - helmdownloader_sonarqube_logs:/opt/sonarqube/logs
      - helmdownloader_sonarqube_extensions:/opt/sonarqube/extensions
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
    networks:
      - sonarqube

  sonar-scanner:
    image: sonarsource/sonar-scanner-cli:12.1.0.3233_8.0.1
    container_name: helmdownloader-sonar-scanner
    restart: "no"
    environment:
      SONAR_HOST_URL: http://sonarqube:9000
    volumes:
      - .:/usr/src
      - helmdownloader_sonar_cache:/opt/sonar-scanner/.sonar
    working_dir: /usr/src
    networks:
      - sonarqube
    depends_on:
      - sonarqube
    profiles:
      - scan

volumes:
  helmdownloader_sonarqube_data:
  helmdownloader_sonarqube_logs:
  helmdownloader_sonarqube_extensions:
  helmdownloader_sonar_cache:

networks:
  sonarqube:
    name: helmdownloader_sonarqube
    driver: bridge
